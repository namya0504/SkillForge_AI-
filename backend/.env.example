# Backend Environment Variables Example
DATABASE_URL="postgresql://postgres.jrikbhexexnmrbtsxikh:uJNe1dqiS1di0fAH@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
JWT_SECRET="skillforge-production-jwt-secret-key-987654321"
JWT_EXPIRES_IN="30m"
PORT=3001
FRONTEND_URL="https://skillforge-ai.vercel.app"
NODE_ENV="production"
MAX_CONCURRENT_JOBS=3
# LLM_API_KEY="sk-..." # Optional OpenAI or Groq API Key

# RUN_WORKER: Set to 'true' ONLY if running the background worker in the same process as the API server (e.g. simple local dev).
# If you have a separate Background Worker service (as in production on Render), leave this unset or 'false' on the Web Service.
RUN_WORKER=true

# Supabase Storage Configuration (Persistent File Storage)
SUPABASE_URL="https://jrikbhexexnmrbtsxikh.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your_service_role_key_here" # backend only, never expose to frontend
